# a consumer of a missing component says what it needs

    Code
      fit_component(older, "intervals", "The Cox-Snell residual")
    Condition
      Error:
      ! The Cox-Snell residual needs the intervals component, which this fit does not carry.
      i It was added after this model was fitted; re-estimate to obtain it.

