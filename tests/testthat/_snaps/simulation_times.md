# times_of refuses what is not a model

    Code
      times_of(data.frame())
    Condition
      Error in `times_of()`:
      ! `times_of()` needs a specification or a fitted model.
      x A <data.frame> was supplied.

