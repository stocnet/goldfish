# a term absent from one process names that process

    Code
      diagnose_outliers(container, effect = "trans")
    Condition
      Error:
      ! `diagnose_outliers()` could not diagnose process "calls › creation › rate".
      Caused by error:
      ! `effect` names a term this model does not have.
      ✖ Unknown: "trans".
      ℹ Available: "Intercept" and "indeg/calls".
      ℹ Search them with `model_terms(fit, pattern = )`.

