% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_opt.R
\name{set_estimation_opt}
\alias{set_estimation_opt}
\title{Control Parameters for Estimation}
\usage{
set_estimation_opt(
  initial_parameters = NULL,
  fixed_parameters = NULL,
  offset_coef = NULL,
  max_iterations = 20,
  convergence_criterion = deprecated(),
  score_tol = 1e-06,
  step_tol = 1e-08,
  initial_damping = NULL,
  damping_increase_factor = 2,
  damping_decrease_factor = 3,
  return_interval_loglik = deprecated(),
  return_probabilities = deprecated(),
  return_event_scores = deprecated(),
  diagnostics = c("loglik", "scores"),
  optimizer = c("newton_raphson", "bfgs", "bhhh", "nelder_mead"),
  engine = c("default_c", "default", "gather_compute")
)
}
\arguments{
\item{initial_parameters}{A numeric vector. It includes initial parameter
values used to initialize the estimation process.
Default is \code{NULL}, which means parameters are initialized at zero,
except for the rate intercept when present.}

\item{fixed_parameters}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}} A numeric vector
of the same length as the number of parameters to be estimated in the model.
\code{NA} values indicate parameters to be estimated,
while numeric values indicate parameters to be fixed at the given value.
For example, if the vector is \code{c(2, NA)} then the first component of the
parameter is fixed to 2 during the estimation process.
Default is \code{NULL} (all parameters are estimated).
Superseded by wrapping the term in \code{offset()} in the model formula and
supplying its value through \code{offset_coef}, which aligns values to terms by
name instead of by counting coefficient positions.}

\item{offset_coef}{A numeric vector giving the fixed coefficient value(s) for
the \code{offset()} term(s) in the model formula, aligned to the offset terms in
formula order. For example, \code{~ inertia + offset(ego(sex)) + recip} with
\code{offset_coef = 2} holds the \code{ego(sex)} coefficient at 2 while estimating the
rest. Default is \code{NULL} (no offsets).}

\item{max_iterations}{An integer non-negative.
The maximum number of iterations in the Gauss-Fisher scoring algorithm.
Default is \code{20}.}

\item{convergence_criterion}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}.
Use \code{score_tol} instead. This argument is ignored.}

\item{score_tol}{A positive numeric value.
Scale-invariant gradient convergence criterion.
The algorithm stops if
\code{max(abs(score)) / max(1, abs(logLik)) <= score_tol}.
Default is \code{1e-6}.}

\item{step_tol}{A positive numeric value.
Damped Newton step size convergence criterion.
The algorithm stops if \code{max(abs(update)) <= step_tol}.
Default is \code{1e-8}.}

\item{initial_damping}{A positive numeric value.
The initial damping factor for the Gauss-Fisher scoring algorithm.
Default is \code{NULL}, which allows \code{estimate_dynam()}, \code{estimate_rem()} and
\code{estimate_dynami()} to set
a context-dependent default
(e.g., 30 or 10 based on wheter the model has windows effects).
If set, this value is used directly.}

\item{damping_increase_factor}{A positive numeric value.
Factor by which damping is increased when improvements
in the estimation are found. Must be >= 1. Default is \code{2}.}

\item{damping_decrease_factor}{A positive numeric value.
Factor by which damping is decreased when no improvements
in the estimation are found. Must be >= 1. Default is \code{3}.}

\item{return_interval_loglik}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Superseded by
\code{diagnostics = "loglik"}. Whether to keep and return the log-likelihood for
each event.}

\item{return_probabilities}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Superseded by
\code{diagnostics = "probabilities"}. Whether to keep and return the
probabilities for all alternatives for each event.
\itemize{
\item When \code{sub_model = "choice"} the probabilities correspond to all actors in
the choice set present at the time of the event.
\item When \code{model = "REM"} the probabilities correspond to all dyads present at
the time of the event.
}}

\item{return_event_scores}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Superseded by
\code{diagnostics = "scores"}. Whether to keep and return the per-event score
matrix (one row per
dependent event, one column per effect) evaluated at the returned
parameter estimates, stored as the \code{event_scores} component of the result.
Each row is the observation-level gradient contribution whose column sums
equal the aggregate score. The matrix supports downstream diagnostics
(implemented by other tools, not here): robust sandwich and clustered
standard errors from the outer product of gradients \code{crossprod(event_scores)},
per-effect score-process diagnostics that localize where individual effects
drift over the event sequence, and event-influence measures. Only the
\code{"default_c"} and \code{"default"} engines support it; \code{"gather_compute"} aborts.}

\item{diagnostics}{Names the per-event diagnostic \emph{primitives} estimation
stores on the fitted result, superseding the three \verb{return_*} flags above.
Accepts a character vector drawn from
\code{c("loglik", "scores", "ranks", "margins", "probabilities")}, or the
shorthands \code{TRUE} (equivalent to \code{c("loglik", "scores")}), \code{"all"} (all
five), and \code{FALSE} / \code{character(0)} (none). Each primitive maps to a stored
component of the result: \code{"loglik"} to \code{intervalLogL} (and \code{total_rate} on
exact-time submodels), \code{"scores"} to \code{event_scores}, \code{"ranks"} to
\code{observed_rank}, \code{"margins"} to per-actor observed and expected counts, and
\code{"probabilities"} to per-event probability vectors. Unknown names abort with
an error listing the valid primitives. Default is \code{c("loglik", "scores")},
preserving today's stored log-likelihood and adding the (free) scores.}

\item{optimizer}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}} A character string
naming the optimization algorithm. Options are:
\describe{
\item{newton_raphson}{The built-in damped Newton-Raphson / Fisher
scoring loop (default).}
\item{bfgs}{Quasi-Newton BFGS, via \code{maxLik::maxLik()}.}
\item{bhhh}{Berndt-Hall-Hall-Hausman, using the per-event score matrix
as observation-level gradients, via \code{maxLik::maxLik()}.}
\item{nelder_mead}{Derivative-free Nelder-Mead, via \code{maxLik::maxLik()}.}
}
Any value other than \code{"newton_raphson"} requires the \pkg{maxLik} package
(in \code{Suggests}) and runs only on the \code{"default_c"} engine. Default is
\code{"newton_raphson"}.}

\item{engine}{A character string specifying the estimation engine.
Options are:
\describe{
\item{default_c}{\verb{C++} based implementation using RcppEigen
and RcppParallel.}
\item{default}{R-based implementation.}
\item{gather_compute}{\verb{C++} based implementation with a different data
structure that reduces the time but it can increase the memory usage.}
}
Default is \code{"default_c"}.}
}
\value{
An object of class \code{estimation_opt.goldfish} (a list object),
where the components values are the default values or the values provided
to the function. The list object has the following components:
\item{initial_parameters}{Initial parameter values used during
the estimation process.}
\item{fixed_parameters}{Values for parameters fixed during
the estimation process.}
\item{max_iterations}{Maximum number of iterations in the
estimation process.}
\item{score_tol}{Scale-invariant gradient convergence criterion for
the estimation process.}
\item{step_tol}{Damped Newton step size convergence criterion for
the estimation process.}
\item{initial_damping}{Initial damping factor for the
estimation process.}
\item{damping_increase_factor}{Factor by which damping is
increased when improvements in the estimation are found.}
\item{damping_decrease_factor}{Factor by which damping is
decreased when no improvements in the estimation are found.}
\item{return_interval_loglik}{Logical value indicating whether to
return the log-likelihood for each event.}
\item{return_event_scores}{Logical value indicating whether to
return the per-event score matrix.}
\item{diagnostics}{Character vector of the diagnostic primitives to store
on the fitted result.}
\item{optimizer}{Optimization algorithm used in the estimation process.}
\item{engine}{Estimation engine used in the estimation process.}
}
\description{
Specifies control parameters for the model estimation process in
\verb{[estimate]}.
}
\details{
The damping factors arguments control the step size at each iteration of
the Newton-Raphson algorithm. They have a bigger impact in the first
iterations of the algorithm and will decrease by half after each iteration.
In particular, the increase factor is the one that is expected to play
a role during the first iterations where it's easier to improve
the log-likelihood.
In scenarios where the model is fit in a large dataset, for example,
when the number of actors in the system is large,
the \code{damping_increase_factor} and the \code{damping_decrease_factor} arguments
can be increased from the default values (e.g., 4 or 6) to speed up the
estimation process with large changes in the coefficients.
However, this should have the opposite effect in small datasets producing
large changes in the coefficients that would create more iterations
(in a similar vein to the step size parameter in gradient descendent).
}
\examples{
est_ctrl <- set_estimation_opt(
  max_iterations = 50,
  score_tol = 1e-7,
  step_tol = 1e-9
)
}
