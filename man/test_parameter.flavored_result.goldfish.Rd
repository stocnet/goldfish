% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_parameter.R
\name{test_parameter.flavored_result.goldfish}
\alias{test_parameter.flavored_result.goldfish}
\title{Score test on a multi-process specification}
\usage{
\method{test_parameter}{flavored_result.goldfish}(x, effects = NULL, preprocessed = NULL, ...)
}
\arguments{
\item{x}{a multi-process fit of class \code{"flavored_result.goldfish"}.}

\item{effects}{an optional selection of the held terms to test, given by any
name a term answers to or by position; \code{\link[=model_terms]{model_terms()}} lists them. A bare
\strong{effect} name selects every term of that effect, so \code{effects = "inertia"} tests each held variant of \code{inertia} the formula carries.
Defaults to every \code{offset()} term the fit carries.}

\item{preprocessed}{a \code{preprocessed.goldfish} object to evaluate over, as
returned by \code{\link[=compute_statistics]{compute_statistics()}}. Defaults to the object attached by
\code{estimate_*(return_preprocessed = TRUE)}; with neither route available the
evaluation aborts naming both, rather than silently recomputing statistics
that might not match.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}
}
\value{
An object of class \code{test_parameter}, shaped as the single-fit result
and documented at \code{\link[=test_parameter.result.goldfish]{test_parameter.result.goldfish()}}, with \code{flavor} and
\code{family} columns appended and one joint row per process in the metadata.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Tests each process of a specification fit against \strong{its own} \code{offset()}
terms, exactly as \code{\link[=test_parameter]{test_parameter()}} tests a single fit.
}
\details{
No candidate argument is needed or accepted per process: each process
formula already declares which of its terms are held, so the tested set is
read off the fit rather than supplied. A process carrying no \code{offset()}
term contributes no rows.

The per-process joint tests are carried in the metadata, one row per
process. They are \strong{not} combined across processes. The processes are
independent, so a sum of chi-squares would be a legitimate statistic — but
it is not one the specification asks for, and a combination nobody has
asked to interpret is not worth reporting.
}
\seealso{
\code{\link[=test_parameter.result.goldfish]{test_parameter.result.goldfish()}} for what each process's test is.
}
