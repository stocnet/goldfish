% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_writers.R
\name{preprocess_writers}
\alias{preprocess_writers}
\title{Preprocessing output writers}
\description{
A writer decouples the recipe event loops from the output
format they produce. Every writer exposes three hooks:
}
\details{
\describe{
\item{\code{init(spec, dims)}}{called once before the event loop with the
model spec and a \code{dims} list describing the problem
(\code{nEffects}, \code{n1}, \code{n2}, \code{is_sender}, \code{buf_capacity}, \code{max_store},
\code{has_intercept}, \code{twomode_or_reflexive}, \code{initial_stats_fn} — a thunk
returning the recipe's \code{initial_stats} once pre-start updates are
applied).}
\item{\code{write_event(event_updates, event_info, event_broadcast)}}{called
once per stored event. \code{event_updates} is a 4 x k matrix of flat point
updates (rows \code{node1}, \code{node2}, \code{effect}, \code{replace}; all 0-indexed)
accumulated since the previous stored event; \code{event_info} is a list with
\code{is_dependent}, \code{interval}, \code{time}, \code{sender}, \code{receiver};
\code{event_broadcast} is the optional 4 x b matrix of compact broadcast
entries (rows \code{kind}, \code{fixed}, \code{effect}, \code{replace}; see the
broadcast-update entry format documented on \code{writer_default()}),
defaulting to an empty 4 x 0 matrix.}
\item{\code{finalize(tail)}}{called once after the loop with a \code{tail} list of
recipe-computed assembly inputs (\code{initial_stats},
\code{active_sender_init} / \code{active_sender_changes},
\code{active_dyad_init} / \code{active_dyad_changes}, \code{start_time}, \code{end_time},
\code{is_exact_time}). Returns the writer's output.}
}

Recipe methods emit output exclusively through these hooks; they never
branch on the output format. \code{compute_statistics(output = ...)} selects the
writer.
}
\section{Future extension points (documented, not implemented)}{

The writer contract is the seam for three planned output strategies that
are deliberately out of scope for this change:
\describe{
\item{Alternatives-sampling gather writer}{a \code{writer_gather()} variant
that keeps gather rows for only a sample of the alternatives per
event. It changes the likelihood contribution (sampled denominators),
so the estimation procedure must be adapted (sampling design and
estimator correction) before it can be used; the writer obligation is
to record, per event, which alternatives were sampled alongside the
selected one.}
\item{Parallel chunk preprocessing}{chunk the event sequence by time
points, warm-start each chunk's \code{init()} from the state at its first
event, run recipe + writer per chunk in parallel, and merge the
per-chunk outputs in a coordinating \code{finalize()}. The obligation is
that \code{write_event} is associative across a contiguous chunk boundary
and \code{finalize} can stitch ordered chunk results.}
\item{Per-event simulation hook}{a hook on the recipe loop (not a
writer) invoked after the stats update for event i and
before advancing: it sees the visible state and may append to the
event stream, reserved for a future \code{simulate()} goodness-of-fit
method. Its interface obligation is the position in the loop, the
visible-state snapshot it receives, and the event-stream append
semantics.}
}
}

\keyword{internal}
