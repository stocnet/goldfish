% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_parameters.R
\name{set_init_param}
\alias{set_init_param}
\title{Set parameter values over a joint specification}
\usage{
set_init_param(spec, ...)
}
\arguments{
\item{spec}{a \code{goldfishJointSpec} (from
\code{\link[=make_joint_specification]{make_joint_specification()}}).}

\item{...}{one full-length per-fid vector per process, keyed by the process's
rendered label; or a single fitted joint result (the from-result form).}
}
\value{
a \code{goldfishParams} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{set_init_param()} builds a self-validating \code{goldfishParams}
object over a \code{goldfishJointSpec}, the parameter surface both
\code{estimate_dynes()} (initial parameters) and \code{simulate()} (generative
coefficients) accept. Each \code{...} argument is a \strong{full-length per-fid
vector} -- one entry per coefficient of that process, in coefficient order
(the intercept when the sub-model carries one, then the effects in formula
order, then the interaction columns) -- keyed by the process's \strong{rendered
label}, the \verb{layer > flavor > family} form that \code{coef()} / \code{print()} surface
for a fitted result (the flavor segment is present only when the process
carries a flavor).

A key is resolved by \strong{membership} against the set of rendered labels, never
by splitting it back into components, so a key matching no label -- or
matching more than one -- aborts naming the valid labels. A per-fid vector
must have exactly the process's coefficient count; its entries may be named
or positional, but \strong{all-or-nothing} -- a partly named vector is rejected,
and a fully named one must name every coefficient exactly once. A process
whose key is \strong{omitted} is read as all-free (\code{NA} at every non-fixed slot).
Fixed
coefficients (\code{offset()} terms and operand-only interaction columns) always
take the specification's value.

Alternatively, \code{set_init_param(spec, result)} accepts a \strong{fitted joint
result} in place of the per-fid vectors -- the fit -> re-simulate
round-trip.
The per-fid values are reconstructed from the result's own \code{\link[=coef_layout]{coef_layout()}}
(which keeps the \code{fid} grouping the flat \code{\link[=coef]{coef()}} vector discards) after
asserting the result was fit against \strong{that same \code{spec}}; a mismatch aborts.
A flat, free-only \code{\link[=coef]{coef()}} vector is \strong{not} accepted directly, because its
per-parameter names collide across fids.
}
\seealso{
\code{\link[=make_joint_specification]{make_joint_specification()}}
}
