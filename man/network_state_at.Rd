% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_at.R
\name{network_state_at}
\alias{network_state_at}
\title{Network state of a layer at a time point}
\usage{
network_state_at(data, layer, time, start_time = -Inf)
}
\arguments{
\item{data}{a \code{stocnet} object (or a \code{\link[=as_goldfish]{as_goldfish()}}-stamped one).}

\item{layer}{character string naming the layer.}

\item{time}{the time point; the returned state reflects updates strictly
before it. Numeric, \code{POSIXct}, \code{Date}, or a character timestamp.}

\item{start_time}{optional lower bound; earlier timed rows fold into the
initial state. \code{time = NA} history rows are always included.}
}
\value{
a numeric adjacency matrix with \code{label} dimnames.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Evaluates a stocnet layer's network as the adjacency matrix implied by all of
its \code{time = NA} history rows plus every timed update strictly before \code{time},
applied under the layer's \code{info$update} semantics (last \code{replace} wins;
\code{increment}s aggregate). This is exactly the initial state preprocessing would
use for \code{start_time} at \code{time}.
}
\examples{
nodes <- data.frame(label = c("A", "B", "C"))
ties <- data.frame(
  from = c(1L, 2L, 3L),
  to = c(2L, 3L, 1L),
  time = c(NA, 1, 2),
  layer = "calls"
)
info <- list(
  focal = "calls", update = c(calls = "increment"),
  directed = c(calls = TRUE), observation = c(calls = "event")
)
x <- list(info = info, nodes = nodes, ties = ties)
network_state_at(x, "calls", time = 2)
}
\seealso{
\code{\link[=nodes_state_at]{nodes_state_at()}}
}
