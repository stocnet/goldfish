% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_spec.R
\name{risk_set_axis}
\alias{risk_set_axis}
\title{Risk-set axis of a fitted model or a model specification}
\usage{
risk_set_axis(x)
}
\arguments{
\item{x}{a fitted model of class \code{"goldfishFit"} (from
\code{\link[=estimate_dynam]{estimate_dynam()}}, \code{\link[=estimate_rem]{estimate_rem()}}), a preprocessed object, or a model
specification.}
}
\value{
A length-one character vector, one of:
\describe{
\item{\code{"sender"}}{rate models — the risk set is the sender set, so a
per-event position is an actor who could have acted.}
\item{\code{"receiver_given_sender"}}{choice models — one sender's receiver
row, so a per-event position is a candidate receiver.}
\item{\code{"dyad"}}{REM, REM-ordered and coordination — the full ordered
dyad grid. Coordination shares this axis because its statistics are
computed on the same grid; that its likelihood sums each unordered
pair once is a property of the likelihood, not of the axis.}
}
\code{NULL} for a fit produced before goldfish 2.0.0, which did not record it;
consumers treat a missing value as an unknown axis, as they do for
\code{backend}.
}
\description{
The axis that gives meaning to a position in a per-event diagnostic component
— \code{event_probabilities}, \code{event_scores}, ranks, margins. Two fits over the
same node set can return per-event vectors of identical length that index
different things: on a DyNAM-rate model position \code{i} is a \emph{sender}, on a
DyNAM-choice model it is a \emph{receiver given the sender}. The axis is what
distinguishes them, so a consumer resolves an index without re-deriving the
model family from \code{model} / \code{sub_model}.
}
\details{
Join a per-event index to the fit's \code{node_lookup} on the side the axis names:
side 1 for \code{"sender"}, side 2 for \code{"receiver_given_sender"} on a two-mode
model. A one-mode model draws both ends of a dyad from side 1, because its
sender and receiver sets are the same nodes, so its lookup carries side 1
only.
}
\examples{
data("social_evolution")
rate <- estimate_dynam(
  calls ~ 1 + indeg + outdeg,
  sub_model = "rate",
  data = social_evolution
)
choice <- estimate_dynam(
  calls ~ inertia + recip,
  sub_model = "choice",
  data = social_evolution
)
# Same node set, same per-event length, different meaning.
risk_set_axis(rate)
risk_set_axis(choice)

}
\seealso{
\code{\link[=estimate_dynam]{estimate_dynam()}} for the fitted object's other components.
}
