% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_gof.R
\name{test_gof.flavored_result.goldfish}
\alias{test_gof.flavored_result.goldfish}
\title{Goodness of fit of a multi-process specification}
\usage{
\method{test_gof}{flavored_result.goldfish}(
  object,
  effects = NULL,
  clock = c("event", "information"),
  n_sim = 1000,
  ...
)
}
\arguments{
\item{object}{a multi-process fit of class \code{"flavored_result.goldfish"}, as
returned by estimating a \code{\link[=make_specification]{make_specification()}} with more than one process.}

\item{effects}{an optional selection of the terms to test, given by any
name a term answers to (the compact string the summary prints, the export
form, the \code{coef()} label) or by position; \code{\link[=model_terms]{model_terms()}} lists them.
Defaults to every free coefficient.}

\item{clock}{which reference the p-values come from, \code{"event"} (default) or
\code{"information"} — see the section above. The statistic is the same under
both.}

\item{n_sim}{the number of replications of the simulated reference on the
information clock. Ignored on the event clock, whose p-value is analytic.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}
}
\value{
An object of class \code{test_gof}, shaped exactly as the single-fit
result and documented at \code{\link[=test_gof.result.goldfish]{test_gof.result.goldfish()}}, with \code{flavor} and
\code{family} columns appended to each component and the joint omnibus in the
metadata. The printed report shows the per-block individual tests; no
combination is rendered at either level.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Tests each process of a specification fit exactly as \code{\link[=test_gof]{test_gof()}} tests a
single fit, and reports the individual per-block results.

A flavored specification is K \emph{independent} fits — the competing-flavor
likelihood factorizes, which is why nothing here is pooled. Each block is a
process: one flavor's one sub-model, with its own effect set, its own event
count and its own standardizing constants. A shared constant across blocks
would assert a joint model that was never estimated.
}
\details{
The components are the same three tibbles a single-fit result carries, each
row-bound over processes with \code{flavor} and \code{family} \strong{appended}, so a plot
method facets on those columns instead of needing a separate flavored
method, and so a consumer never has to ask whether a flavored result nested
one level deeper.

\code{omnibus} gains one row per block — that block's Cauchy combination over
its own effects. The \strong{joint} omnibus is a property of the whole object
rather than of any row, so it lives in the metadata, reachable as
\code{attr(x, "context")$joint}. Neither is printed: the printed report is the
per-block individual tests, for the reason given under
\code{\link[=test_gof.result.goldfish]{test_gof.result.goldfish()}}'s experimental section.

The joint combination is taken over the \strong{effect-level} p-values of every
block, not over the per-block omnibus values. A block carrying more effects
therefore contributes more of the combination, which is the reading that
treats an effect rather than a process as the unit being combined. The
Cauchy combination is valid under arbitrary dependence either way.

It is also dominated by its most extreme input, in \strong{both} directions: one
effect with a very small p-value drags the combination toward 0, and one
whose process is unusually flat — a p-value near 1 — drags it toward 1,
far enough to mask a significant effect elsewhere. That is the combination
behaving as defined rather than a defect, but it means the per-block and
per-effect rows are what to read when the joint value is uninformative.

\code{effects} is matched against each process separately, since the processes
have different formulas. Naming a term some process does not carry is an
error that says which process, rather than a silent omission.
}
\seealso{
\code{\link[=test_gof.result.goldfish]{test_gof.result.goldfish()}} for what each block's test is.
}
