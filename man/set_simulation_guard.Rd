% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_guard.R
\name{set_simulation_guard}
\alias{set_simulation_guard}
\title{Set the guards that stop a runaway simulation}
\usage{
set_simulation_guard(
  max_events = NULL,
  rate_multiple = Inf,
  wait_collapse = Inf,
  wait_window = 50L,
  clock_resolution = 0
)
}
\arguments{
\item{max_events}{the count guard, over proposed events: \code{NULL} for ten
times the number of observed dependent events, or one positive whole
number.}

\item{rate_multiple}{stop when the total rate exceeds this multiple of its
value at the first drawn event. One number of at least 1; \code{Inf}, the
default, never fires.}

\item{wait_collapse}{stop when the median of the last \code{wait_window} waiting
times falls below the observed mean waiting time divided by this factor.
One number of at least 1; \code{Inf}, the default, never fires.}

\item{wait_window}{the number of most recent waiting times whose median
\code{wait_collapse} reads. One positive whole number.}

\item{clock_resolution}{stop when a step moves the clock by no more than
this fraction of the window length. \code{0}, the default, stops only when a
wait leaves the clock unchanged; \code{NULL} turns the stop off, so collapsed
timestamps run on to \code{max_events}.}
}
\value{
An object of class \code{goldfishSimGuard}, a list with components:
\item{max_events}{Value from the \code{max_events} argument, as an integer.}
\item{rate_multiple}{Value from the \code{rate_multiple} argument.}
\item{wait_collapse}{Value from the \code{wait_collapse} argument.}
\item{wait_window}{Value from the \code{wait_window} argument, as an integer.}
\item{clock_resolution}{Value from the \code{clock_resolution} argument.}
}
\description{
A free-running replicate stops at its target, \code{n_events} or \code{horizon}, or
earlier at a guard. The guards are set here and passed to \code{\link[=simulate]{simulate()}} as
\code{control_sim}. A guard stop ends only its own replicate: the events drawn
so far are kept, the replicate is flagged \code{capped}, and it stays in the
result.
}
\details{
By default a replicate whose total rate grows without bound runs on to
\code{max_events}, so the explosion shows in the run itself. Two guards are on
by default. \code{max_events} caps the proposed events, and \code{clock_resolution}
stops a replicate once a drawn waiting time no longer moves the clock,
after which every event would share one timestamp. The two
rate-trajectory triggers, \code{rate_multiple} and \code{wait_collapse}, are off
until set, for a user who wants a runaway to stop earlier.

The window length that \code{clock_resolution} is measured against runs from
the start to the end of the observation window: the \code{start_time} and
\code{end_time} of \code{\link[=set_preprocessing]{set_preprocessing()}} when set, otherwise the first and last
observed row on the schedule, not counting a windowed effect's expiries.
The observed mean waiting time is that length over the number of observed
dependent events.
}
\examples{
# Stop a replicate once its total rate is a thousand times its start.
set_simulation_guard(rate_multiple = 1e3)
}
\seealso{
\code{\link[=simulate]{simulate()}}, \code{\link[=filter_simulation]{filter_simulation()}}.

Other simulation:
\code{\link[=filter_simulation]{filter_simulation()}},
\code{\link[=set_parameter_provider]{set_parameter_provider()}},
\code{\link[=set_simulation_steps]{set_simulation_steps()}},
\code{\link{simulate}},
\code{\link{simulation-handle}},
\code{\link[=times_of]{times_of()}}
}
\concept{simulation}
