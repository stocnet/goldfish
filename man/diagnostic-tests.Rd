% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_tests.R
\name{diagnostic-tests}
\alias{diagnostic-tests}
\alias{test_gof}
\alias{test_parameter}
\alias{test_time}
\title{Diagnostic tests for a fitted model}
\usage{
test_gof(object, ...)

test_parameter(x, ...)

test_time(x, ...)
}
\arguments{
\item{object}{a fitted model of class \code{"result.goldfish"}, or a
specification (multi-process) fit. Named \code{object} rather than \code{x} on
\code{\link[migraph:test_gof]{test_gof()}} alone, matching the signature the sibling generic already
publishes.}

\item{...}{additional arguments passed to or from other methods.}

\item{x}{a fitted model of class \code{"result.goldfish"}, or a specification
(multi-process) fit.}
}
\value{
An object of the test's own class, carrying the metadata described
in \link{diagnostic-tables}.
}
\description{
Three questions a fitted relational event model can be asked, each an S3
generic dispatching on the fit:
\describe{
\item{\code{\link[migraph:test_gof]{test_gof()}}}{does the model fit the sequence it was estimated on?
Read from the cumulative score processes, which are bridges at the
maximum: a process that wanders is an effect whose contribution is not
spread over the sequence the way the model assumes.}
\item{\code{\link[=test_parameter]{test_parameter()}}}{is a coefficient held at an imposed value
consistent with the data? The score test of the terms a fit carries as
\code{offset()}, evaluated at the vector those terms were fixed to.}
\item{\code{\link[=test_time]{test_time()}}}{is a coefficient constant over the sequence? Read
either as a trend in the scaled Schoenfeld residuals against a time
transform, or as a comparison of period-wise partial sums.}
}
}
\details{
The three names are shared with \pkg{RSiena}, which publishes generics of
the same names for the same three questions asked of a different model
class. That is a convergence rather than a collision — but R finds a method
through the generic in scope, so with both packages attached the one
attached later masks the other's generic and the masked package's methods
become invisible to it. Call \code{goldfish::test_gof()} and its siblings
explicitly if both are attached; that always resolves.
}
\seealso{
\code{\link[=diagnose_outliers]{diagnose_outliers()}} and \code{\link[=diagnose_changepoints]{diagnose_changepoints()}} for the
descriptive counterparts, \code{\link[=residuals.result.goldfish]{residuals.result.goldfish()}} for the residuals
these read, and \link{diagnostic-tables} for the metadata a result carries.
}
