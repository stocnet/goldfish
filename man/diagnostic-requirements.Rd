% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_tables.R
\name{diagnostic-requirements}
\alias{diagnostic-requirements}
\title{What a diagnostic needs from a fit}
\description{
The diagnostics divide into three entry costs, and which one a function has
is the first thing to know about it: some read what the fit already carries,
some need a primitive that had to be requested at estimation, and some need
the model's statistics and one evaluation pass over them. This page is the
shared vocabulary; each function's own page states where it falls.
}
\section{What a diagnostic needs}{
Three things can be asked of a fit, in increasing order of what they cost to
have available.

\describe{
\item{\strong{Always present}}{the coefficients, the total information matrix,
the interval clock (\code{intervals}, \code{start_time}, \code{end_time}), the event
times and the right-censoring indicator. These ride on every fit
regardless of what was requested, so a diagnostic reading only these has
no requirement to state.}
\item{\strong{A stored primitive}}{a per-interval quantity accumulated during
estimation and kept on the result, requested through
\code{set_algorithm_newton(diagnostics = )}: \code{"loglik"}, \code{"scores"},
\code{"ranks"}, \code{"margins"}, \code{"availability"}, \code{"probabilities"},
\code{"conditional_scores"}. A primitive not requested is not recoverable
from the fit — it was never computed — so a diagnostic needing one
aborts naming the primitive, and the remedy is to re-estimate with it in
the \code{diagnostics} vector. The default is \code{c("loglik", "scores")}.}
\item{\strong{The model's statistics}}{the preprocessed design the model was
estimated from, which a fit does not carry by default because it is
large. Two routes make it available: \code{estimate_*(return_preprocessed = TRUE)} attaches it to the result, and \verb{preprocessed = } supplies an
equivalent object from \code{\link[=compute_statistics]{compute_statistics()}} to the diagnostic
directly. A diagnostic needing it aborts naming \strong{both} routes.}
}
}

\section{What a pass costs}{
A diagnostic that needs the statistics needs them because it recomputes
something the fit does not store, and that recomputation is \strong{one
evaluation pass} — a single walk of the event sequence through
\code{\link[=evaluate_model]{evaluate_model()}}, with no Newton-Raphson iterations. It is the cost of one
likelihood evaluation, not of a re-fit. What forces a pass is a per-interval
quantity no primitive holds: the per-interval expected information is the
clearest case, since a fit keeps only the total.
}

\section{The family side by side}{
\tabular{lll}{
\strong{Function} \tab \strong{Stored primitive} \tab \strong{Statistics / passes} \cr
\code{\link[=test_gof]{test_gof()}} \tab \code{"scores"} \tab none \cr
\code{\link[=test_parameter]{test_parameter()}} \tab none \tab required, 1 pass \cr
\code{\link[=test_time]{test_time()}} \tab none \tab required, 1 pass \cr
\code{\link[=diagnose_outliers]{diagnose_outliers()}} \tab \code{"loglik"} \tab none \cr
\code{\link[=diagnose_changepoints]{diagnose_changepoints()}} \tab \code{"loglik"} or \code{"scores"} \tab none \cr
\code{\link[=diagnose_onset]{diagnose_onset()}} \tab \code{"scores"} \tab \code{information = "expected"} only,
1 pass \cr
\code{\link[=residuals]{residuals()}} \tab varies by \code{type} \tab varies by \code{type} \cr
\code{\link[=fitted]{fitted()}} / \code{\link[=predict]{predict()}} \tab none \tab required, 1 pass \cr
\code{\link[=margin_table]{margin_table()}} \tab \code{"margins"} \tab none \cr
\code{\link[=evaluate_model]{evaluate_model()}} \tab none \tab required, 1 pass \cr
}

Two readings the table is for. The \verb{test_*} family's entry costs are
\strong{uneven} — \code{test_gof()} needs a primitive and no pass, the other two need
the statistics — which is a consequence of what each statistic is rather
than an inconsistency: a cumulative score process is a reduction of stored
score rows, while an augmented-model score test needs per-interval
information that is stored nowhere. And a \code{residuals()} \code{type} spans the
whole range on its own, from \code{"cox_snell"} (the interval clock and
\code{"loglik"}, no pass) to \code{"dfbeta"} (the statistics and a pass).
}

\seealso{
\code{\link[=set_algorithm_newton]{set_algorithm_newton()}} for requesting a primitive,
\code{\link[=compute_statistics]{compute_statistics()}} for producing the statistics, \code{\link[=evaluate_model]{evaluate_model()}}
for the pass itself, and \link{diagnostic-tables} for what the returned object
carries.
}
