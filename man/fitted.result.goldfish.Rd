% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_predict.R
\name{fitted.result.goldfish}
\alias{fitted.result.goldfish}
\title{Fitted values of a goldfish model}
\usage{
\method{fitted}{result.goldfish}(object, type = c("outcome", "probabilities"), preprocessed = NULL, ...)
}
\arguments{
\item{object}{a fitted model of class \code{"result.goldfish"}.}

\item{type}{the quantity to return, \code{"outcome"} (default) or
\code{"probabilities"}.}

\item{preprocessed}{a \code{goldfishStat} object to recompute from, as
returned by \code{\link[=compute_statistics]{compute_statistics()}}. Only the recomputing types read it, and
only when the fit did not store what they need; it defaults to the object
attached by \code{estimate_*(return_preprocessed = TRUE)}.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}
}
\value{
For \code{"outcome"}, a numeric vector with one value per interval. For
\code{"probabilities"}, a list with one per-event vector or dyad grid over the
whole node set, zero off that interval's risk set.
}
\description{
The model's own fitted quantities at the observed decision points:
\code{"outcome"} (the default) is the fitted probability — for the exact-time
sub-models, the density contribution — of each interval's realized outcome,
and \code{"probabilities"} is the full fitted vector over the alternatives at
each of them.
}
\details{
\code{"outcome"} is \code{exp()} of the stored per-interval log-likelihood, so it
costs nothing: a fit that stored the \code{"loglik"} primitive (the default) can
always produce it. \code{"probabilities"} is the per-event vector over the whole
node set, read from the fit when it stored the \code{"probabilities"} primitive
and otherwise recomputed through one evaluation pass. That primitive is the
one estimation warns about the size of, so recomputing rather than storing
is the ordinary case.
}
\examples{
data("social_evolution")
fit <- estimate_dynam(
  calls ~ inertia + recip,
  sub_model = "choice",
  data = social_evolution
)
# How probable was what actually happened, event by event?
summary(fitted(fit))

}
\seealso{
\code{\link[=predict.result.goldfish]{predict.result.goldfish()}} for the same quantities under the
prediction vocabulary, \code{\link[=residuals.result.goldfish]{residuals.result.goldfish()}} for what the model
got wrong.
}
