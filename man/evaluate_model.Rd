% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_evaluate.R
\name{evaluate_model}
\alias{evaluate_model}
\alias{evaluate_model.result.goldfish}
\title{Evaluate a fitted model at a parameter vector}
\usage{
evaluate_model(x, ...)

\method{evaluate_model}{result.goldfish}(
  x,
  at = stats::coef(x),
  return = c("loglik", "score"),
  preprocessed = NULL,
  weights = NULL,
  backend = NULL,
  recall_at = c(1L, 5L, 10L),
  ...
)
}
\arguments{
\item{x}{a fitted model of class \code{"result.goldfish"}.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}

\item{at}{the parameter vector to evaluate at. Either a full-length numeric
vector in coefficient order, or a named one matched against the
coefficient labels the model reports, in which case unnamed coefficients
keep their fitted value. Defaults to the fitted coefficients.}

\item{return}{a character vector naming the quantities to compute, any
subset of \code{"loglik"}, \code{"score"}, \code{"information"},
\code{"weighted_information"}, \code{"event_information_trace"},
\code{"interval_loglik"},
\code{"total_rate"}, \code{"conditional_logl"}, \code{"event_scores"},
\code{"conditional_scores"}, \code{"ranks"}, \code{"recall"}, \code{"margins"},
\code{"exposure"}, \code{"n_opportunities"} and \code{"probabilities"}. The returned
list carries exactly these, in that order.
The components mean what the same-named components of a fitted object
mean; see \code{\link[=estimate_dynam]{estimate_dynam()}}.}

\item{preprocessed}{a \code{preprocessed.goldfish} object to evaluate over, as
returned by \code{\link[=compute_statistics]{compute_statistics()}}. Defaults to the one attached to the
fit.}

\item{weights}{a numeric matrix with one row per stored interval, required
by \code{"weighted_information"} and ignored otherwise. Each column is a
separate weighting of the per-interval expected information, and its name
labels the corresponding slice of the returned array. A column of ones
reproduces \code{"information"}; a set of disjoint indicator columns gives the
per-group blocks, at the cost of one accumulation per interval rather than
one per column, since zero weights are skipped.}

\item{backend}{the computational implementation to evaluate on, defaulting
to the one that produced the fit. Requesting a quantity a backend cannot
produce aborts naming the backends that can.}

\item{recall_at}{an integer vector of thresholds for \code{"recall"}: the
proportion of dependent events whose realized alternative ranked within
the top \code{k}.}
}
\value{
A named list carrying the requested quantities, plus \code{backend}, the
implementation that produced them, and \code{at}, the vector they were
evaluated at. \code{"weighted_information"} is a \verb{p x p x m} array whose \code{m}-th
slice is \verb{sum_k weights[k, m] * I_k}, with \code{I_k} the interval's expected
information contribution, and whose third dimension carries
\code{colnames(weights)}; \code{"event_information_trace"} is the length-\code{n} vector
of \code{trace(I_k)}, which sums to the trace of \code{"information"}. Neither
materializes the per-interval blocks: both accumulate inside the one pass.
\code{"recall"} is a named numeric vector, one proportion per
threshold; \code{"margins"} is the per-actor list documented under
\code{\link[=estimate_dynam]{estimate_dynam()}}, labeled and scale-marked as a fit's own margins are.
\code{"exposure"} (the per-actor time at risk, summed over every interval
including the right-censored ones) and \code{"n_opportunities"} (the number of
dependent events whose realized risk set contained the actor) are labeled
by actor: one numeric vector on the sender, receiver and endpoint
families, and a two-element \verb{_sender} / \verb{_receiver} list on the two-sided
REM families, which is the shape their margins take. Both count per actor
membership, so an actor at risk in many dyads of one interval contributes
that interval once. \code{"exposure"} is defined only for the exact-time
sub-models; requesting it elsewhere aborts naming \code{"n_opportunities"}.
}
\description{
Runs the fitted model's likelihood machinery \strong{once} at the parameter
vector \code{at} — no Newton-Raphson iterations, no re-estimation — and returns
the quantities asked for. It is the recompute half of the diagnostics
surface: what a fit did not store can be produced at the maximum likelihood
estimate, at a constrained vector, or anywhere else, without refitting.
}
\details{
The evaluation reuses the statistics the model was estimated from, so it
never re-runs preprocessing: they come from the object attached by
\code{estimate_*(return_preprocessed = TRUE)}, or from an equivalent one supplied
through \code{preprocessed}. Without either, the evaluation aborts naming both
routes rather than silently recomputing statistics that might not match.

Evaluation is \strong{unconditional on fixedness}: at a vector holding some
coefficients at fixed values, the returned score and information are those
of the full model at that vector, with the dimensions of the full effect
set. Nothing is zeroed out — that is what makes the output usable as the
constrained-model input of a score test.
}
\examples{
data("social_evolution")
fit <- estimate_dynam(
  calls ~ inertia + recip + trans,
  sub_model = "choice",
  data = social_evolution,
  return_preprocessed = TRUE
)
# At the maximum the score is (near) zero by construction.
evaluate_model(fit, return = c("loglik", "score"))

# Anywhere else it is not: the full-model score at a vector holding the
# third coefficient at zero is what a score test reads.
constrained <- coef(fit)
constrained[3] <- 0
evaluate_model(fit, at = constrained, return = c("score", "information"))

}
\seealso{
\code{\link[=estimate_dynam]{estimate_dynam()}} for the stored primitives an evaluation
complements, \code{\link[=compute_statistics]{compute_statistics()}} for the statistics it reads.
}
