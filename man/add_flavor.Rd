% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_flavor.R
\name{add_flavor}
\alias{add_flavor}
\title{Mark a layer's ties with competing-process flavors}
\usage{
add_flavor(x, layer, values_equivalence, flavor_style = "mutually_exclusive")
}
\arguments{
\item{x}{a \code{stocnet} object (see \link{goldfish_data}).}

\item{layer}{character(1) naming the layer whose ties to flavor.}

\item{values_equivalence}{a named numeric vector mapping flavor names to the
layer's update values, e.g. \code{c(creation = 1, dissolution = -1)} for an
increment layer or \code{c(creation = 1, dissolution = 0)} for a replace layer.
Names must be syntactic R names -- they become \code{\link[=make_specification]{make_specification()}}
formula-list keys.}

\item{flavor_style}{one of \code{"mutually_exclusive"} (competing directions on
one state, the default -- creation and dissolution cannot both apply to a
dyad, so a specification derives complementary support masks) or
\code{"redundant"} (repeated same-direction events are meaningful, so no
constraint is derived).}
}
\value{
\code{x} with \code{ties$flavor} stamped for \code{layer}, and the mapping and style
recorded in \code{info$values_equivalence} and \code{info$flavor_style}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
A relational-state layer -- friendship, alliances, treaties -- typically
evolves through competing sub-processes: ties are \strong{created} and later
\strong{dissolved}. \code{add_flavor()} records that reading on a \code{stocnet} object so a
\code{\link[=make_specification]{make_specification()}} flavor-keyed \code{rate} / \code{choice} list can model each
sub-process as its own process. It is deliberately thin: it stamps the
reserved \code{ties$flavor} column from each event's update value and records the
mapping and style in the layer \code{info}. It derives \strong{no} support constraints
-- specification time owns that (a \code{mutually_exclusive} layer's
create-only-where-absent / dissolve-only-where-present masks are derived by
\code{\link[=make_specification]{make_specification()}}).

Only \strong{dichotomous} states are supported: increment layers whose events are
\code{+1} / \code{-1}, and replace layers whose events set \code{1} / \code{0}. A weighted layer
(increments other than \code{+-1}), a multistate mapping, or a mapping that does
not match the layer's update encoding aborts, because those updates
accumulate or replace raw values rather than toggling a state; see the
\code{weighted} argument of effect terms for the usual compromise.

Only timed event rows are flavored; \code{time = NA} history rows seed the initial
state and stay unflavored.
}
\examples{
\dontshow{if (rlang::is_installed("manynet")) withAutoprint(\{ # examplesIf}
data("fisheries_treaties")
fish <- add_flavor(
  fisheries_treaties,
  layer = "treaties",
  values_equivalence = c(creation = 1, dissolution = -1)
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=make_specification]{make_specification()}} for modeling flavors; \link{goldfish_data} for the
data object and the reserved \code{flavor} column.
}
