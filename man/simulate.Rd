% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_methods.R
\name{simulate}
\alias{simulate}
\alias{simulate.goldfishJointSpec}
\alias{simulate.goldfishSpec}
\alias{simulate.goldfishFit}
\alias{simulate.goldfishFlavFit}
\title{Simulate event sequences from a specification or a fitted model}
\usage{
\method{simulate}{goldfishJointSpec}(
  object,
  nsim = 1,
  seed = NULL,
  coef = NULL,
  times = times_of(object),
  n_events = NULL,
  horizon = NULL,
  max_events = NULL,
  steps = NULL,
  control_prep = set_preprocessing(),
  ...
)

\method{simulate}{goldfishSpec}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{goldfishFit}(object, nsim = 1, seed = NULL, data = NULL, coef = NULL, ...)

\method{simulate}{goldfishFlavFit}(object, nsim = 1, seed = NULL, data = NULL, coef = NULL, ...)
}
\arguments{
\item{object}{a specification (\code{\link[=make_specification]{make_specification()}} or
\code{\link[=make_joint_specification]{make_joint_specification()}}) or a fitted model.}

\item{nsim}{number of replicates to simulate.}

\item{seed}{passed to \code{\link[=set.seed]{set.seed()}} before the first replicate, for a
reproducible run.}

\item{coef}{the parameters to simulate at: a numeric vector for a
single-process specification, a \code{\link[=set_parameters]{set_parameters()}} object for a joint one,
or a \code{\link[=set_parameter_provider]{set_parameter_provider()}}. A fitted model supplies its own estimates
and needs none.}

\item{times}{\code{"generated"} for a free-running run or \code{"observed"} for a
time-anchored one. Defaults to \code{\link[=times_of]{times_of()}}, which reads the variant from
what the model can honor; an explicit value it cannot honor is refused
with the reason.}

\item{n_events}{stop after this many events.}

\item{horizon}{stop at this time.}

\item{max_events}{the explosion guard; defaults to ten times the number of
observed dependent events. A run reaching it stops and is flagged.}

\item{steps}{a \code{\link[=set_simulation_steps]{set_simulation_steps()}} object replacing any of the driver's
own steps.}

\item{control_prep}{preprocessing options, from \code{\link[=set_preprocessing]{set_preprocessing()}}.}

\item{...}{passed between methods.}

\item{data}{the stocnet to simulate on. Required on a fitted model, which
stores its formula and estimates but not the data it was fitted on.}
}
\value{
For \code{nsim = 1} a \code{goldfishSim}: the simulated \code{events}, the
\code{process_map} with each process's regime, the \code{times} variant and its
\code{times_source} (\code{"specification"} or \code{"requested"}), the \code{capped} flag and
run \code{diagnostics}. For \code{nsim > 1} a list of them.
}
\description{
Generates event sequences from a goldfish model: the waiting time and the
marks are drawn from the model at the state the previous event left behind,
so the simulated sequence feeds back into its own statistics exactly as an
observed one does.
}
\details{
Two variants, named by \code{times}. \strong{Free-running} (\code{"generated"}) draws the
clock and the marks from the model, holding nothing from the observed stream
but the initial state — this is what tests the clock. \strong{Time-anchored}
(\code{"observed"}) holds the observed event times and redraws only the marks.
The default is read from the model by \code{\link[=times_of]{times_of()}}: free-running when it
carries a timed rate, time-anchored when its rate is ordered, it coordinates,
or it has no rate.

Observed exogenous streams — covariate changes and node composition — march
along with the simulated clock. A free-running run that passes the last
observed change holds that state and warns once rather than inventing its
continuation.

A run stops at whichever target binds first, \code{n_events} or \code{horizon}. The
\code{max_events} guard is not a target: it is the explosion backstop, and a run
that reaches it is flagged \code{capped} so a pool can exclude it.

Every step of the loop is replaceable — see \code{\link[=set_simulation_steps]{set_simulation_steps()}} for the
plug points and \code{\link[=set_parameter_provider]{set_parameter_provider()}} for parameters that change during
a run.
}
\examples{
\donttest{
data("Social_Evolution", package = "goldfish")
# A specification simulates at parameters you choose.
# simulate(spec, nsim = 1, coef = c(-8, 0.1), n_events = 50)
}
}
\seealso{
\code{\link[=set_simulation_steps]{set_simulation_steps()}}, \code{\link[=set_parameter_provider]{set_parameter_provider()}},
\link{simulation-handle}.

Other simulation:
\code{\link[=set_parameter_provider]{set_parameter_provider()}},
\code{\link[=set_simulation_steps]{set_simulation_steps()}},
\code{\link{simulation-handle}},
\code{\link[=times_of]{times_of()}}
}
\concept{simulation}
