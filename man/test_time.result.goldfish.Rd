% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_time.R
\name{test_time.result.goldfish}
\alias{test_time.result.goldfish}
\alias{test_time.flavored_result.goldfish}
\alias{print.test_time}
\title{Test whether an effect's coefficient is constant over the sequence}
\usage{
\method{test_time}{result.goldfish}(
  x,
  method = c("trend", "periods"),
  transform = c("identity", "rank"),
  periods = 4L,
  effects = NULL,
  preprocessed = NULL,
  ...
)

\method{test_time}{flavored_result.goldfish}(
  x,
  method = c("trend", "periods"),
  transform = c("identity", "rank"),
  periods = 4L,
  effects = NULL,
  preprocessed = NULL,
  ...
)

\method{print}{test_time}(x, ...)
}
\arguments{
\item{x}{a fitted model of class \code{"result.goldfish"}; for the print method,
the \code{test_time} object it renders.}

\item{method}{\code{"trend"} (default) for a smooth drift in the coefficient, or
\code{"periods"} for a difference between regimes.}

\item{transform}{the time transform \code{g} used by \code{"trend"}: \code{"identity"}
(default) or \code{"rank"}. Ignored by \code{"periods"}.}

\item{periods}{what defines the regimes for \code{"periods"}, in one of three
forms told apart by shape:
\describe{
\item{a single whole number \code{J}}{splits the sequence into \code{J} groups of
approximately equal \strong{dependent-event} counts. This is the default
form (\code{4L}).}
\item{cut times}{a numeric vector of two or more values, or a single
fractional one, read as boundaries on the model's own clock and giving
right-open intervals. A count and a single cut time are distinguished
by something a count cannot be — a count is whole — and a cut falling
\emph{between} two events is in any case the only kind that splits the
sequence unambiguously, so \code{periods = 200.5} is one cut and
\code{periods = 200} is a request for two hundred periods.}
\item{a grouping}{a vector or factor with one entry per interval, used
directly, which is how an exogenous regime is supplied.}
}
Ignored by \code{"trend"}.}

\item{effects}{an optional selection of the terms to test, given by any
name a term answers to (the compact string the summary prints, the export
form, the \code{coef()} label) or by position; \code{\link[=model_terms]{model_terms()}} lists them.
A bare \strong{effect} name selects every term of that effect, so
\code{effects = "inertia"} tests all of \code{inertia}, \code{inertia(friendship)} and
\code{inertia(calls, weighted = TRUE)} on a model carrying the three.
Defaults to every free coefficient.}

\item{preprocessed}{a \code{goldfishStat} object to evaluate over, as
returned by \code{\link[=compute_statistics]{compute_statistics()}}. Defaults to the object attached by
\code{estimate_*(return_preprocessed = TRUE)}; with neither route available the
evaluation aborts naming both, rather than silently recomputing statistics
that might not match.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}
}
\value{
An object of class \code{test_time}: a list of three
\code{\link[tibble:tibble]{tibble::tibble()}}s, carrying the metadata described in
\link{diagnostic-tables}.
\describe{
\item{\code{effects}}{one row per tested effect — \code{statistic}, \code{df} and
\code{p_value} of its augmentation, under the term's compact string, plus
\code{rank}, the term's position by descending statistic with \code{1} the most
extreme. The rank is a column rather than the row order, so a script
can take the front of the table without a screen while the rows stay
in model order — and, on a multi-process fit, ranked within each
process. \code{df} is constant within a call, which is what makes the
statistics comparable enough to rank.}
\item{\code{residuals}}{plot-ready per-interval data: the model's clock, the
transformed clock, the scaled Schoenfeld residual of each tested
effect, and the fitted estimate it should scatter around. Under
\code{"periods"} it also carries the interval's period.}
\item{\code{periods}}{the one-step per-period coefficient deltas, one row per
tested effect and period, with the reference period at zero. Empty
under \code{"trend"}, which has no periods.}
}
The joint test over all tested effects is in \code{attr(x, "context")$global},
and the print method reports it.
}
\description{
A fitted coefficient is one number standing for the whole observation
window. \code{test_time()} asks whether that is tenable: whether the effect the
model estimated once was in fact the same effect throughout.

Both methods are \strong{exact score tests of an augmented model} — the fitted
model plus an interaction between the effect and time — evaluated at the
fitted coefficients, where the augmentation's score is what the fitted model
left on the table. \code{"trend"} augments with a smooth function of time and
asks whether the coefficient drifts; \code{"periods"} augments with period
indicators and asks whether it differs between regimes.
}
\section{What the two methods are}{
\describe{
\item{\code{"trend"}}{augments the model with \code{x_d * g(t)} for a time
transform \code{g}, and tests the interaction's coefficient against zero:
one degree of freedom per effect, a directed alternative, most powerful
against a coefficient that moves monotonically. This is the
proportional-hazards test of survival analysis, and on a sub-model whose
classical twin is a Cox partial likelihood it agrees with
\code{survival::cox.zph()}.}
\item{\code{"periods"}}{augments it with \verb{x_d * 1\{k in period j\}} for
\verb{j = 2..J}, the first period being the reference: \code{J - 1} degrees of
freedom per effect, an undirected alternative, and the method to reach
for when a regime change is expected at a known time rather than a drift.
Its per-period one-step deltas are the interpretable readout — what the
coefficient would move to in each period.}
}
}

\section{Which clock the transform is applied to}{
The time axis is \strong{the one the sub-model's own likelihood runs on}, not
wall-clock time in every case. An ordinal sub-model conditions on the order
of events and nothing else, so its clock is the event index; an exact-time
sub-model integrates a compensator over elapsed time, so its clock is the
event time. Applying a transform to an axis the likelihood never saw gives a
number that answers no question the model poses — and it is not a small
effect: on the package's own Cox-equivalent fixture, using wall-clock times
where the likelihood is ordinal moves the \code{"identity"} statistic by 24\%.

A consequence worth stating: on an ordinal sub-model \code{"identity"} and
\code{"rank"} are the same transform, because the event index already is the
rank.
}

\section{What it requires}{
One \code{\link[=evaluate_model]{evaluate_model()}} pass, and therefore the model's statistics — attached
by \code{estimate_*(return_preprocessed = TRUE)} or supplied through
\code{preprocessed}. Without either it aborts naming both routes. The reason is
that the augmented model's information needs the \strong{per-interval} expected
information, which no fitted object stores: a fit keeps the total.

There is deliberately no outer-product option. An outer-product form would
need no pass, but it is an approximation of the quantity being tested, and a
p-value is a claim: the cheap variants available here are a Lagrange
multiplier test that over-rejects in finite samples, and the
scaled-Schoenfeld regression that \code{survival} itself retired when \code{cox.zph()}
was rewritten as an exact test. \code{\link[=diagnose_onset]{diagnose_onset()}} keeps its outer-product
default because a cumulative share is a description, not a test.
}

\section{What this test is not}{
It is not a test of a \strong{windowed statistic}. \code{inertia(net, window = 300)}
asks whether the \emph{statistic} should forget old events — a hypothesis about
memory, which changes the design matrix and therefore needs preprocessing.
Fit both and compare, or hold the windowed variant at zero with
\code{offset(..., coef = 0)} and use \code{\link[=test_parameter]{test_parameter()}}. \code{test_time()} holds the
statistic fixed and asks about the coefficient on it.
}

\section{What this test needs}{


The model's \strong{statistics}, and \strong{one evaluation pass}. No stored primitive:
the augmented model's information needs the per-interval expected
information, which no fit carries — a fit keeps the total.
}

\section{What a diagnostic needs}{
Three things can be asked of a fit, in increasing order of what they cost to
have available.

\describe{
\item{\strong{Always present}}{the coefficients, the total information matrix,
the interval clock (\code{intervals}, \code{start_time}, \code{end_time}), the event
times and the right-censoring indicator. These ride on every fit
regardless of what was requested, so a diagnostic reading only these has
no requirement to state.}
\item{\strong{A stored primitive}}{a per-interval quantity accumulated during
estimation and kept on the result, requested through
\code{set_algorithm_newton(diagnostics = )}: \code{"loglik"}, \code{"scores"},
\code{"ranks"}, \code{"margins"}, \code{"availability"}, \code{"probabilities"},
\code{"conditional_scores"}. A primitive not requested is not recoverable
from the fit — it was never computed — so a diagnostic needing one
aborts naming the primitive, and the remedy is to re-estimate with it in
the \code{diagnostics} vector. The default is \code{c("loglik", "scores")}.}
\item{\strong{The model's statistics}}{the preprocessed design the model was
estimated from, which a fit does not carry by default because it is
large. Two routes make it available: \code{estimate_*(return_preprocessed = TRUE)} attaches it to the result, and \verb{preprocessed = } supplies an
equivalent object from \code{\link[=compute_statistics]{compute_statistics()}} to the diagnostic
directly. A diagnostic needing it aborts naming \strong{both} routes.}
}
}

\examples{
data("social_evolution")
fit <- estimate_dynam(
  calls ~ inertia + recip,
  sub_model = "choice",
  data = social_evolution,
  return_preprocessed = TRUE
)
test_time(fit)

# A regime split, with the coefficient's one-step move in each period.
regimes <- test_time(fit, method = "periods", periods = 2L)
regimes$periods

}
\seealso{
\code{\link[=test_gof]{test_gof()}} for whether an effect's contribution is spread over
the sequence as the model assumes, \code{\link[=test_parameter]{test_parameter()}} for a coefficient
held at an imposed value, \code{\link[=diagnose_changepoints]{diagnose_changepoints()}} for locating a shift
rather than testing one, and \link{diagnostic-tables} for the metadata a
diagnostic object carries.
}
