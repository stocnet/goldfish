% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_specification.R
\name{make_specification}
\alias{make_specification}
\title{Build a model specification object}
\usage{
make_specification(
  rate = NULL,
  choice = NULL,
  model = c("DyNAM", "REM"),
  rate_sub_model = c("rate", "rate_ordered"),
  choice_sub_model = c("choice", "choice_coordination"),
  layer = NULL,
  support_constraint = NULL,
  data = NULL
)
}
\arguments{
\item{rate}{a one-sided formula (empty left-hand side) with the rate-model
effects, a flavor-keyed list of one such formula, or \code{NULL}.}

\item{choice}{a one-sided formula (empty left-hand side) with the
choice-model effects, a flavor-keyed list of one such formula, or \code{NULL}.
Not applicable to \code{model = "REM"}.}

\item{model}{a character string, either \code{"DyNAM"} or \code{"REM"}. DyNAM-i is
deferred to its own preprocessing-path change.}

\item{rate_sub_model}{a character string, the rate sub-model:
\code{"rate"} or \code{"rate_ordered"}.}

\item{choice_sub_model}{a character string, the choice sub-model:
\code{"choice"} or \code{"choice_coordination"}.}

\item{layer}{a character string naming the dependent process: a layer of the
\code{stocnet} object, defaulting to its \code{info$focal}.}

\item{support_constraint}{a one-sided formula restricting the per-event risk
set, written in a restricted boolean-tree grammar. The leaves are effect
\emph{atoms} (\code{tie(net)}, \code{indeg(net)}, ...) combined into a logical expression
with:
\itemize{
\item boolean operators \code{&}, \code{|}, \code{!};
\item comparisons \verb{> < >= <= == !=}, either effect-vs-constant
(\code{indeg(net) > 2}) or effect-vs-effect (\code{indeg(net) > outdeg(net)});
\item elementwise arithmetic \verb{+ - * /} (with parentheses);
\item a bare effect, which means \code{effect != 0}.
}

A cell \verb{(i, j)} is in the risk set when the expression evaluates \code{TRUE}
there. It is parsed and validated at construction; the restriction is
applied during estimation.

\strong{\code{*} is elementwise arithmetic here, not interaction expansion.} In an
\emph{effects} formula \code{a * b} expands to \code{a + b + a:b} (estimated interaction
columns); in a \emph{constraint} \code{a * b} is the elementwise product of the two
atoms' values, with no columns and no coefficients. For 0/1 indicators
\code{a * b} coincides with \code{a & b}, and \code{&} is the clearer idiom — prefer
\code{tie(a) & tie(b)} over \code{tie(a) * tie(b)}. \code{NULL} by default.}

\item{data}{a \code{stocnet} object, raw or stamped by \code{\link[=as_goldfish]{as_goldfish()}}. It is
validated here either way: a stamp records provenance, not validity.}
}
\value{
an S3 object of class \code{specification.goldfish}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{make_specification()} bundles the rate and/or choice formulas of a model
together with its \code{model}, sub-model(s), the dependent process (named by
\code{layer}), an optional \code{support_constraint}, and the \code{data} object, returning a
reusable \code{specification.goldfish} object. The object holds the parsed formula
bundles that preprocessing consumes, so it can be passed directly to
\code{\link[=estimate_dynam]{estimate_dynam()}} / \code{\link[=estimate_rem]{estimate_rem()}} in place of a formula.

Unlike the formula interface (\code{estimate_dynam(dep ~ ...)}), the rate and
choice formulas leave the \strong{left-hand side empty}; the dependent process is
named by \code{layer}. For a \code{stocnet} object \code{layer} names one of its layers and
defaults to \code{info$focal}.
}
\section{Modeling flavors of a layer}{
When a layer's ties carry a \code{flavor} column, \code{rate} / \code{choice} may be given
as a \strong{flavor-keyed list} whose formula left-hand side is the flavor to
model, e.g. \code{rate = list(creation ~ 1 + indeg())}. Only rows of that flavor
are modeled as events; every other focal row -- including rows with no
flavor -- still updates the network state. Supplying a plain formula on a
flavored layer models \strong{all} of its rows, and says so.

Several flavors can be modeled as parallel competing processes on the same
focal layer, e.g. \code{rate = list(creation ~ ..., dissolution ~ ...)}. Each key
becomes its own process with its own formulas and, on a \code{mutually_exclusive}
layer (see \code{\link[=add_flavor]{add_flavor()}}), a derived support constraint -- creation is
supportable only where no tie exists, dissolution only where one does --
AND-composed with any \code{support_constraint}. When both \code{rate} and \code{choice} are
keyed lists they MUST key the same flavor set. An unflavored layer under a
flavor-keyed list infers the mapping from its update semantics
(increment \code{+1}/\code{-1}, replace \code{1}/\code{0}) and says so.
}

\examples{
# The prebuilt `social_evolution` stocnet, whose focal layer is `calls`
data("social_evolution")
spec <- make_specification(
  choice = ~ inertia + recip + trans,
  model = "DyNAM", choice_sub_model = "choice",
  layer = "calls", data = social_evolution
)
spec
}
\seealso{
\code{\link[=estimate_dynam]{estimate_dynam()}}, \code{\link[=estimate_rem]{estimate_rem()}}, \code{\link[=make_dependent_events]{make_dependent_events()}},
\code{\link[=make_data]{make_data()}}
}
