% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pool.R
\name{filter_simulation}
\alias{filter_simulation}
\alias{summary.goldfishSimPool}
\title{Filter a pool of simulated replicates}
\usage{
filter_simulation(pool, ...)

\method{summary}{goldfishSimPool}(object, ...)
}
\arguments{
\item{pool}{a \code{goldfishSimPool}, from \code{\link[=simulate]{simulate()}} with \code{nsim > 1}.}

\item{...}{conditions evaluated against \code{summary(pool)}, each giving one
logical per replicate. Several conditions are combined with AND, and a
replicate whose condition is \code{NA} is dropped. Variables that are not
summary columns are read from the calling environment.}

\item{object}{a \code{goldfishSimPool}.}
}
\value{
A \code{goldfishSimPool} of the kept replicates, keeping their original
replicate numbers. \code{pool} itself is unchanged.

\code{summary()} on a pool returns the data frame the conditions read, one row
per replicate: \code{replicate}, \code{n_events}, \code{end_time} (the clock when the
run stopped), \code{stop_reason}, \code{capped}, \code{n_proposals} and
\code{acceptance_rate}.
}
\description{
Keeps the replicates of a \code{\link[=simulate]{simulate()}} pool whose per-replicate summary
meets every condition. Nothing is excluded from a pool by default: a
replicate that stopped at a guard stays in it, and in anything computed
from it, until a filter removes it, so the criterion is written in the
call that applies it.
}
\examples{
\donttest{
# pool <- simulate(spec, nsim = 100, coef = coef)
# filter_simulation(pool, stop_reason == "horizon")
# filter_simulation(pool, !capped, n_events > 100)
}
}
\seealso{
Other simulation:
\code{\link[=set_parameter_provider]{set_parameter_provider()}},
\code{\link[=set_simulation_guard]{set_simulation_guard()}},
\code{\link[=set_simulation_steps]{set_simulation_steps()}},
\code{\link{simulate}},
\code{\link{simulation-handle}},
\code{\link[=times_of]{times_of()}}
}
\concept{simulation}
