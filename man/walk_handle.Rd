% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/walk_handle.R
\name{walk_handle}
\alias{walk_handle}
\alias{walk_open}
\alias{walk_advance}
\alias{walk_inject}
\alias{walk_evaluate}
\title{Open, step, evaluate, and inject over the merged single-clock walk}
\usage{
walk_open(
  spec,
  control_preprocessing = set_preprocessing_opt(),
  call = rlang::caller_env()
)

walk_advance(handle, t, call = rlang::caller_env())

walk_inject(handle, event, call = rlang::caller_env())

walk_evaluate(handle, fid, theta, call = rlang::caller_env())
}
\arguments{
\item{spec}{a \code{goldfishJointSpec} from
\code{\link[=make_joint_specification]{make_joint_specification()}} (or a single \code{goldfishSpec}, wrapped
as a one-process join).}

\item{control_preprocessing}{preprocessing options, as for
\code{\link[=preprocess_joint]{preprocess_joint()}}.}

\item{call}{the calling environment, for error reporting.}

\item{handle}{a \code{goldfishWalk} from \code{\link[=walk_open]{walk_open()}}.}

\item{t}{the clock time to advance to.}

\item{event}{a list describing one event to apply: \code{layer} (the object name),
plus \code{sender} / \code{receiver} (a dyadic layer) or \code{node} (a nodal attribute),
an \code{increment} or \code{replace} value, and an optional \code{time} (defaults to the
handle's current clock).}

\item{fid}{the integer formula id to evaluate (a row of the specification's
\code{process_map}).}

\item{theta}{the parameter vector for the fid, matching its effect columns
(with a leading intercept for a timed rate).}
}
\value{
\code{walk_open()} returns a \code{goldfishWalk} object.

\code{walk_advance()} returns the handle invisibly.

\code{walk_inject()} returns the handle invisibly.

\code{walk_evaluate()} returns the process-state evaluator result for the
fid: an \code{index} data frame and the per-alternative \code{value} (a rate vector
for a sender-block fid, a choice matrix's probabilities for a dyad-block
fid), the fid's support applied.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A stateful developer substrate over the merged multivariate walk, for a
generative driver (the DyNES augmenter, a future \code{simulate()}) that must step
the clock, evaluate a process at the live state, and apply sampled or
observed events mid-walk. \code{walk_open()} builds the handle; \code{walk_advance()}
moves the clock applying exogenous events; \code{walk_evaluate()} returns a fid's
rate vector or choice matrix at the current state and parameters (its
support applied); \code{walk_inject()} applies one event to the shared state so
every consumer sees it.

These are internal building blocks: not exported, committing to no
user-facing signature. \code{walk_open()} \strong{asserts} its specification is
generatively complete (every modeled DyNAM flavor carrying both a rate and a
choice) and aborts pointing to \code{\link[=simulate]{simulate()}} / \code{estimate_dynes()} otherwise;
it never performs completion itself.
}
\keyword{internal}
