% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_steps.R
\name{set_simulation_steps}
\alias{set_simulation_steps}
\title{Supply the steps of the simulation loop}
\usage{
set_simulation_steps(
  parameters = NULL,
  evaluate = NULL,
  clock = NULL,
  mark = NULL,
  accept = NULL
)
}
\arguments{
\item{parameters}{a \code{\link[=set_parameter_provider]{set_parameter_provider()}} object, or \code{NULL}.}

\item{evaluate}{a function of \verb{(stats, theta, risk, meta)}, or \code{NULL} for
goldfish's own evaluator.}

\item{clock}{a function of \verb{(rates, t, handle)}, or \code{NULL} for the clock the
specification's behavior selects.}

\item{mark}{a function of \verb{(fid, evaluation, handle)}, or \code{NULL} for the
family's own mark.}

\item{accept}{a function of \verb{(event, handle)}, or \code{NULL} to accept every
drawn event.}
}
\value{
An object of class \code{goldfishSimSteps}, a list with components:
\item{parameters}{Value from the \code{parameters} argument.}
\item{evaluate}{Value from the \code{evaluate} argument.}
\item{clock}{Value from the \code{clock} argument.}
\item{mark}{Value from the \code{mark} argument.}
\item{accept}{Value from the \code{accept} argument.}
}
\description{
Builds the object \code{\link[=simulate]{simulate()}} takes as \code{steps}, replacing any of the
driver's own steps with the caller's. Every slot left \code{NULL} keeps
goldfish's step for that point, chosen from the specification's behavior, so
a caller overrides only what its model changes.
}
\details{
The contracts, in the order one step of the loop calls them:

\describe{
\item{\code{parameters}}{a \code{\link[=set_parameter_provider]{set_parameter_provider()}} object. Supplying it here
is equivalent to passing it as \code{coef}; \code{coef} wins if both are given.}
\item{\code{evaluate}}{\verb{function(stats, theta, risk, meta)} returning the
values over the process's candidate space — hazards for a timed family,
probabilities for a multinomial one, exact zeros outside the risk set.
\code{stats} is the process's statistics rows in sender-major order, \code{theta}
the parameters as the provider returned them, \code{risk} the live risk set
(\code{active_sender}, \code{active_dyad}, \code{encoding}), and \code{meta} a list naming
the \code{family}, \code{n_actors1}, \code{n_actors2} and, for one sender's choice row,
the \code{sender}. This is where a model variant lives: a per-actor random
effect is a row-wise product over a matrix \code{theta}, a regime mixture a
weighted sum, a non-log-linear hazard a different link.}
\item{\code{clock}}{\verb{function(rates, t, handle)} returning
\code{list(wait, fid, kind)} — the waiting time, which process fires, and
whether the draw is an \code{"event"} or a \code{"breakpoint"}. \code{rates} is the
per-fid total rate at the current state.}
\item{\code{mark}}{\verb{function(fid, evaluation, handle)} returning the event to
inject: a list with \code{layer}, and \code{sender}/\code{receiver} (a dyadic layer) or
\code{node}, plus \code{increment} or \code{replace}.}
\item{\code{accept}}{\verb{function(event, handle)} returning \code{TRUE} to inject the
event. The default always accepts; an endpoint-conditioned augmenter
rejects what cannot reach its endpoint.}
}

Each supplied closure is checked at construction — its argument count, and a
dry call on a one-actor toy problem — so a contract mistake surfaces here
rather than midway through a replicate.
}
\examples{
# An evaluate step giving each actor its own parameter row.
steps <- set_simulation_steps(
  evaluate = function(stats, theta, risk, meta) {
    rows_per_ego <- nrow(stats) / nrow(theta)
    ego <- rep(seq_len(nrow(theta)), each = rows_per_ego)
    linear <- rowSums(stats * theta[ego, , drop = FALSE])
    values <- numeric(length(linear))
    values[risk$active_sender] <- exp(linear[risk$active_sender])
    values
  }
)
class(steps)
}
\seealso{
\code{\link[=set_parameter_provider]{set_parameter_provider()}}, \code{\link[=simulate]{simulate()}}.

Other simulation:
\code{\link[=set_parameter_provider]{set_parameter_provider()}},
\code{\link{simulate}},
\code{\link{simulation-handle}}
}
\concept{simulation}
