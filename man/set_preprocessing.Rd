% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_opt.R
\name{set_preprocessing}
\alias{set_preprocessing}
\title{Control Parameters for Preprocessing}
\usage{
set_preprocessing(
  start_time = NULL,
  end_time = NULL,
  opportunities_list = NULL,
  impute = NULL,
  db = NULL,
  db_table = "stats"
)
}
\arguments{
\item{start_time}{A numerical value or a date-time character string
(parsable by \code{as.POSIXct}) indicating the starting time when the events
are considered for likelihood computation.
All the events that happen before the \code{start_time} are used to compute
the initial values of the effects statistics in the model.
It's useful to set this parameter when the model has windowed effects or
effects that depends of previous order of events (e.g., \code{trans()} and
\code{cycle()} when \code{history} argument is set to sequential or consecutive,
as they are initialized with empty values.
\code{estimate_dynami()} does not support an observation window and currently
ignores this argument.
Default is \code{NULL} (start from the first event).}

\item{end_time}{A numerical value or a date-time character string
(parsable by \code{as.POSIXct}) indicating the end time when the events
are not to be considered for likelihood computation.
The preprocessing stage stops traversing the event schedule at this time,
so events after it are neither stored nor visited.
When the schedule ends before this time, the elapsed time in between is
exposure during which no event occurred. The sub-models whose likelihood
defines a compensator, the exact-time \code{rate} and REM families, record it
as a right-censored interval, so the accumulated intervals reach
\code{end_time}; the multinomial families record nothing, a censored interval
there contributing exactly zero.
\code{estimate_dynami()} does not support an observation window and currently
ignores this argument.
Default is \code{NULL} (end with the last event).}

\item{opportunities_list}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} A list object.
For choice models, this list specifies, for each dependent event,
the set of available nodes in the choice set.
The list should have the same length as the number of events in the
dependent events objects created with \code{make_dependent_events()}.
Default is \code{NULL}, so the choice set is the set of all nodes present at the
time of the event. Superseded by the \code{support_constraint} argument of
\code{\link[=estimate_dynam]{estimate_dynam()}} / \code{\link[=make_specification]{make_specification()}}, which generalizes it to a
per-\verb{(sender, receiver)} risk-set restriction and works on every engine; an
equivalent constraint over an allowed-dyad network reproduces the
opportunity-list coefficients.}

\item{impute}{An optional named character vector declaring a per-attribute
imputation policy, keyed by nodal attribute name (e.g.
\code{impute = c(party = "as_category")}). Every attribute not named uses the
default summary contract documented in the \emph{Missing data} section of
\link{goldfish_data}, so omitting \code{impute} leaves behavior unchanged. Supported
values: \code{"summary"} (the default rule) and \code{"as_category"} (recode a
factor or character attribute's missing values to a reserved \code{"(missing)"}
level, so missingness by design survives to the summarizers). \code{"locf"} is
reserved for a future estimator and currently aborts as unimplemented.
Default is \code{NULL}.}

\item{db}{A \code{DBIConnection} object or \code{NULL} (default). When supplied
together with \code{compute_statistics(..., output = "db")}, the gather statistics
are streamed to the database tables prefixed by \code{db_table} instead of
being held in memory.}

\item{db_table}{A single character string naming the table prefix of the
export written when a \code{db} connection is configured. Default is
\code{"stats"}. Every export writes one statistics table per modeled process,
\verb{<db_table>_<fid>} (a specification without flavors is process 1), plus
\verb{<db_table>_map} naming those tables and \verb{<db_table>_nodes} resolving the
\code{index_i} / \code{index_j} columns back to the original nodes.}
}
\value{
An object of class \code{preprocessing.goldfish} (a list object), with
where the components values are the default values or the values provided
to the function. The list object has the following components:
\item{start_time}{Value from \code{start_time} argument.}
\item{end_time}{Value from \code{end_time} argument.}
\item{opportunities_list}{Value from \code{opportunities_list} argument.}
\item{impute}{Value from \code{impute} argument.}
}
\description{
Specifies control parameters for the data preprocessing stage,
used by \code{estimate_dynam()}, \code{estimate_rem()} and \code{estimate_dynami()}
(when \code{preprocessed} is not a
\code{preprocessed.goldfish} object) and \code{\link[=compute_statistics]{compute_statistics()}}.
}
\examples{
prep_ctrl <- set_preprocessing(
  start_time = "2000-01-01 00:00:00",
  end_time = "2000-12-31 23:59:59"
)
}
