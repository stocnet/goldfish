% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_goldfish.R
\name{as_goldfish}
\alias{as_goldfish}
\title{Validate and stamp a stocnet object for goldfish}
\usage{
as_goldfish(x, focal = NULL, ...)
}
\arguments{
\item{x}{a \code{stocnet} object (a list of \code{info}, \code{nodes}, \code{ties}, and optional
\code{changes} / \code{global} components, as produced by
\code{\link[manynet:make_stocnet]{manynet::make_stocnet()}}). A hand-built list with the same structure
(plain data frames) is also accepted -- the boundary reads structure, not
class provenance.}

\item{focal}{optional character string naming the focal (dependent) layer,
overriding \code{info$focal}.}

\item{...}{reserved for future use.}
}
\value{
\code{x}, unchanged in structure, with \code{"data.goldfish"} prepended to its
class vector.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{as_goldfish()} is the optional early gate for the goldfish data input. It
runs the same validation that \code{estimate_dynam()}, \code{estimate_rem()}, and
\code{make_specification()} apply to a \code{stocnet} object, failing fast with an
informative error when the data does not meet goldfish's contract, and stamps
the object with the \code{data.goldfish} class marker.

Passing the stamped object to estimation is equivalent to passing the raw
stocnet: validation runs again unconditionally at specification time. The
stamp is used for provenance and print dispatch only; it does not certify the
object as still valid, because manynet manipulation verbs and plain list
assignment mutate the object while preserving its class vector.
}
\section{Event-ordering contract}{
stocnet coercions arrange ties by \code{from}/\code{to}, discarding the incoming row
order, so goldfish imposes a deterministic event schedule at conversion time
(in preprocessing, not in \code{as_goldfish()}). Events are ordered by:
\enumerate{
\item \code{time};
\item dependent (focal-layer) events before exogenous events;
\item component order: \code{ties}, then \code{changes}, then \code{global};
\item \code{layer};
\item \code{from}/\code{to}.
}

When \code{ties} or \code{changes} carry an integer \code{order} column, it replaces
\code{from}/\code{to} as the final tie-break. Multiple \code{replace} events targeting the
same cell (or the same node-variable) at the same time with no \code{order} column
are genuinely ambiguous and abort with an error naming the colliding rows;
same-time increments commute and pass silently.
}

\examples{
nodes <- data.frame(label = c("A", "B", "C"), floor = c(1, 2, 1))
ties <- data.frame(
  from = c(1L, 2L, 3L),
  to = c(2L, 3L, 1L),
  time = c(NA, 1, 2),
  layer = "calls"
)
info <- list(
  name = "toy",
  focal = "calls",
  update = c(calls = "increment"),
  directed = c(calls = TRUE),
  observation = c(calls = "event")
)
toy <- list(info = info, nodes = nodes, ties = ties)
as_goldfish(toy)
}
