% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_tables.R
\name{margin_table}
\alias{margin_table}
\title{Observed and expected per-actor event counts}
\usage{
margin_table(x, ...)
}
\arguments{
\item{x}{a fitted model of class \code{"result.goldfish"} or
\code{"flavored_result.goldfish"}, estimated with \code{"margins"} among the
\code{\link[=set_algorithm_newton]{set_algorithm_newton()}} \code{diagnostics} primitives.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}

\item{dispersion}{whether to add the \code{dispersion} column, which needs one
evaluation pass over the model's statistics. \code{FALSE} by default, so the
ordinary call stays a read of what estimation already stored.}

\item{preprocessed}{a \code{preprocessed.goldfish} object, as returned by
\code{\link[=compute_statistics]{compute_statistics()}}. Read only when \code{dispersion = TRUE}, and defaulting
to the object attached by \code{estimate_*(return_preprocessed = TRUE)}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of class \code{margin_table} with columns
\describe{
\item{actor}{the actor label, from the node set the margin indexes.}
\item{role}{\code{"sender"}, \code{"receiver"} or \code{"endpoint"}, as above.}
\item{observed}{the actor's tabulated count in the dependent events.}
\item{expected_probability}{the probability-scale expected count.}
\item{expected_count}{the compensator-scale expected count, \code{NA} on the
multinomial families.}
\item{dispersion}{present only when \code{dispersion = TRUE}: the variance of
the actor's own compensators, \code{NA} on the multinomial families (which
define no waiting time) and wherever the actor completed fewer than two
spans. See \emph{Level and shape}.}
}
A multi-process (flavored) fit adds \code{flavor} and \code{family} columns from the
fit's process map. The object carries the diagnostic metadata described in
\link{diagnostic-tables}.
}
\description{
Presents the margins a fit stored (\code{diagnostics = "margins"}) as one table
with the same five columns for every model family, so a calibration
comparison of observed against expected actor activity reads the same way on
a DyNAM rate, a DyNAM choice, a coordination and a REM fit.

These are \emph{descriptives}, not per-actor tests: the differences are plug-in
quantities and are negatively correlated across actors. Read them as a map
screening for unmodeled actor heterogeneity, and test a candidate
explanation by adding the corresponding effect to the model.
}
\details{
The two expected columns are the two scales on which an actor's expected
activity is defined:
\describe{
\item{\code{expected_probability}}{the sum, over the dependent events, of the
fitted probability that this actor is the one involved in the next
event. It totals the number of events at \emph{any} parameter vector, so
\code{observed / expected_probability} is a calibration ratio that can be
read on a model that has not converged.}
\item{\code{expected_count}}{the compensator: the fitted intensity integrated
over the actor's exposure time, accumulated over all intervals including
the right-censored ones. \code{observed - expected_count} is the per-actor
martingale residual. It totals the number of events at the maximum
likelihood estimate, through the time-intercept score equation.}
}

\code{expected_count} is \code{NA} on the multinomial families (DyNAM choice, the
ordinal rate and REM sub-models, and choice coordination). The \code{NA} means the
compensator scale \strong{is not defined for the model class}, never that it was
not computed: those models condition on an event having occurred and carry no
exposure-time term. Such a fit therefore has a calibration ratio but no
martingale residual. Both columns are always present, so the schema does not
change with the family.

The \code{role} column names what an actor did in the events being counted:
\describe{
\item{\code{sender}}{rate sub-models, and the sender side of a REM fit.}
\item{\code{receiver}}{choice sub-models, and the receiver side of a REM fit.}
\item{\code{endpoint}}{choice coordination, where each event credits both
members of the pair -- so the observed column totals twice the event
count.}
}
A REM fit contributes both a \code{sender} and a \code{receiver} row per actor from
the same fit: a tie-oriented model implies both the out- and the in-degree
margin.
}
\section{Level and shape}{

\code{observed} and \code{expected_count} are a \strong{first-moment} view: they say whether
an actor acted as often as the model expected. An actor whose events are
correctly counted but bunched into a burst is calibrated on both of them, and
\code{dispersion} is what separates the two readings:

\if{html}{\out{<div class="sourceCode">}}\preformatted{  well-timed   spans (1.0, 1.0, 1.0, 1.0)      sum 4, n 4   dispersion 0.00
  bursty       spans (0.01, 0.02, 0.01, 3.96)  sum 4, n 4   dispersion 3.90
}\if{html}{\out{</div>}}

It is the variance of that actor's own compensators — the same series
\code{residuals(type = "cox_snell", level = "actor")} returns — over the spans
between its consecutive events. Each is unit exponential under a correct
model, so the column reads against \strong{one} the way \code{observed} reads against
\code{expected_count}.

Two things it is not. It is \strong{not defined below two completed spans}, so an
actor with fewer than two events is \code{NA}, and on a real event stream that is
most actors: it is read beside \code{observed} and never on its own. And the final
span, running to the end of the observation window, is excluded — it closes
no event, so counting it would drag an actor's variance toward zero in
proportion to how early it stopped acting.
}

\examples{
data("social_evolution")
choice <- estimate_dynam(
  calls ~ inertia + recip + trans,
  sub_model = "choice",
  data = social_evolution,
  control_algo = set_algorithm_newton(
    diagnostics = c("loglik", "margins")
  )
)
margin_table(choice)

}
\seealso{
\code{\link[=estimate_dynam]{estimate_dynam()}} and \code{\link[=estimate_rem]{estimate_rem()}} for fitting,
\code{\link[=set_algorithm_newton]{set_algorithm_newton()}} for requesting the primitive, and
\link{diagnostic-tables} for the metadata a diagnostic table carries.
}
