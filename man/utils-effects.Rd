% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_effects.R
\name{utils-effects}
\alias{utils-effects}
\alias{to_ego}
\alias{to_alter}
\title{Expand 2-column rate changes to 3-column ego or alter perspective}
\usage{
to_ego(changes, n2, is_two_mode = FALSE)

to_alter(changes, n1, is_two_mode = FALSE)
}
\arguments{
\item{changes}{a matrix with columns \code{node1} and \code{replace}, or \code{NULL}.}

\item{n2}{integer. Number of alters / receivers (used by \code{to_ego()}).}

\item{is_two_mode}{logical. Whether the network is two-mode; if \code{FALSE}
(default) the focal actor is excluded from the expanded rows.}

\item{n1}{integer. Number of senders / egos (used by \code{to_alter()}).}
}
\value{
A matrix with columns \code{node1}, \code{node2}, \code{replace}, or \code{NULL} when
\code{changes} is \code{NULL}.
}
\description{
Helper functions used internally to promote actor-level change matrices
(2 columns: \code{node1}, \code{replace}) produced by DyNAM-rate effects into the
dyad-level format (3 columns: \code{node1}, \code{node2}, \code{replace}) expected by
choice and REM effect wrappers.
}
\details{
\code{to_ego()} repeats each affected actor across all alters (ego perspective).
\code{to_alter()} repeats each affected actor across all egos (alter perspective).
}
\keyword{internal}
