% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_predict.R
\name{predict.goldfishFit}
\alias{predict.goldfishFit}
\title{In-sample prediction from a fitted goldfish model}
\usage{
\method{predict}{goldfishFit}(
  object,
  type = c("probabilities", "ranks"),
  events = NULL,
  at = NULL,
  preprocessed = NULL,
  ...
)
}
\arguments{
\item{object}{a fitted model of class \code{"goldfishFit"}.}

\item{type}{the quantity to predict, \code{"probabilities"} (default) or
\code{"ranks"}.}

\item{events}{an optional index vector selecting the intervals to report,
either integer positions or a logical mask over all intervals. Defaults to
all of them.}

\item{at}{an optional parameter vector to predict at, in the form
\code{\link[=evaluate_model]{evaluate_model()}} accepts. Defaults to the fitted coefficients.}

\item{preprocessed}{a \code{goldfishStat} object to recompute from, as
returned by \code{\link[=compute_statistics]{compute_statistics()}}. Only the recomputing types read it, and
only when the fit did not store what they need; it defaults to the object
attached by \code{estimate_*(return_preprocessed = TRUE)}.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}
}
\value{
For \code{"ranks"}, an integer vector with the realized alternative's
rank in each interval, \code{1} being the most likely one and \code{NA} on a
right-censored interval, which realizes none. For \code{"probabilities"}, a
list with one per-event vector or dyad grid over the whole node set.
Either is subset to \code{events} when that is given.
}
\description{
What the fitted model says at the observed decision points: the probability
it assigns to each alternative, or the rank it gives the alternative that
was actually realized.
}
\details{
This is \strong{in-sample prediction given the observed history}, not
forecasting. Every quantity is evaluated at a decision point the data
contains, with the endogenous statistics as the observed sequence made
them — so \code{type = "ranks"} answers "how well would the model have called
this event, knowing everything up to it", not "what happens next". Rolling
the process forward requires simulating the events that would have
followed, which is a different computation and not this method. These are
also not marginal effects: a probability here is conditional on that
interval's realized risk set.

At the fitted estimate both quantities come off the fit when it stored the
primitive they need — \code{"ranks"} from \code{observed_rank}, \code{"probabilities"}
from \code{event_probabilities} — and are recomputed through one evaluation pass
otherwise. Supplying \code{at} always evaluates, since the stored values belong
to the fitted coefficients.
}
\section{What these need}{


The model's \strong{statistics} and \strong{one evaluation pass}: a fitted value is a
per-alternative quantity over the realized risk set, which no fit stores
unless \code{"probabilities"} was requested, and which is in any case recomputed
at whatever parameter vector is asked for.
}

\section{What a diagnostic needs}{
Three things can be asked of a fit, in increasing order of what they cost to
have available.

\describe{
\item{\strong{Always present}}{the coefficients, the total information matrix,
the interval clock (\code{intervals}, \code{start_time}, \code{end_time}), the event
times and the right-censoring indicator. These ride on every fit
regardless of what was requested, so a diagnostic reading only these has
no requirement to state.}
\item{\strong{A stored primitive}}{a per-interval quantity accumulated during
estimation and kept on the result, requested through
\code{set_algorithm_newton(diagnostics = )}: \code{"loglik"}, \code{"scores"},
\code{"ranks"}, \code{"margins"}, \code{"availability"}, \code{"probabilities"},
\code{"conditional_scores"}. A primitive not requested is not recoverable
from the fit — it was never computed — so a diagnostic needing one
aborts naming the primitive, and the remedy is to re-estimate with it in
the \code{diagnostics} vector. The default is \code{c("loglik", "scores")}.}
\item{\strong{The model's statistics}}{the preprocessed design the model was
estimated from, which a fit does not carry by default because it is
large. Two routes make it available: \code{estimate_*(return_preprocessed = TRUE)} attaches it to the result, and \verb{preprocessed = } supplies an
equivalent object from \code{\link[=compute_statistics]{compute_statistics()}} to the diagnostic
directly. A diagnostic needing it aborts naming \strong{both} routes.}
}
}

\examples{
data("social_evolution")
fit <- estimate_dynam(
  calls ~ inertia + recip,
  sub_model = "choice",
  data = social_evolution,
  control_algo = set_algorithm_newton(diagnostics = c("loglik", "ranks"))
)
# How often was the actor who was actually called the model's first guess?
mean(predict(fit, type = "ranks") == 1)

}
\seealso{
\code{\link[=fitted.goldfishFit]{fitted.goldfishFit()}} for the same quantities under the
fitted-value vocabulary, \code{\link[=evaluate_model]{evaluate_model()}} for evaluation at any
parameter vector.
}
