% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_times.R
\name{times_of}
\alias{times_of}
\alias{times_of.goldfishJointSpec}
\alias{times_of.goldfishSpec}
\alias{times_of.goldfishFit}
\alias{times_of.goldfishFlavFit}
\alias{times_of.default}
\title{The simulation variant a model supports by default}
\usage{
times_of(object)

\method{times_of}{goldfishJointSpec}(object)

\method{times_of}{goldfishSpec}(object)

\method{times_of}{goldfishFit}(object)

\method{times_of}{goldfishFlavFit}(object)

\method{times_of}{default}(object)
}
\arguments{
\item{object}{a specification (\code{\link[=make_specification]{make_specification()}} or
\code{\link[=make_joint_specification]{make_joint_specification()}}) or a fitted model.}
}
\value{
\code{"generated"} or \code{"observed"}, with a \code{reason} attribute naming the
rule that decided it: \code{"timed rate"}, \code{"ordered rate"}, \code{"coordination"}
or \code{"no rate"}.
}
\description{
\code{times_of()} reads which \code{times} variant \code{\link[=simulate]{simulate()}} uses when none is
given: \code{"generated"} (free-running, the waiting times drawn from the model)
when the model carries a timed rate, and \code{"observed"} (time-anchored, the
observed event times held) otherwise.
}
\details{
The rule, by what the model's processes carry:
\describe{
\item{a timed rate}{\code{"generated"}. The rate is a clock, so the waiting
times are drawn from it.}
\item{an ordered rate}{\code{"observed"}. An ordered rate models which event
comes next, not when, so there is no clock to draw from, and asking for
\code{"generated"} is an error.}
\item{a coordination process}{\code{"observed"}, for the same reason: its
timing is estimated through the order of events, and the rate of the
proposals behind them is not identified. Asking for \code{"generated"} is an
error.}
\item{no rate}{\code{"observed"}. A choice-only DyNAM has no clock. Asking for
\code{"generated"} completes a constant exponential rate pinned at the crude
rate of the observed events, with a warning.}
}

A process of a joint specification whose rate is missing while another
process carries a timed rate is completed with a pinned rate at
\code{\link[=simulate]{simulate()}}'s entry, so such a specification reads as \code{"generated"}. A
flavored fit reads across all of its processes the same way.
}
\examples{
data("social_evolution")
# A choice-only specification has no clock, so it is time-anchored:
choice_only <- make_specification(
  choice = ~ inertia + recip,
  model = "DyNAM",
  layer = "calls",
  data = social_evolution
)
times_of(choice_only)

# A timed rate is a clock, so the waiting times are generated:
timed <- make_specification(
  rate = ~ 1 + indeg,
  choice = ~ inertia + recip,
  model = "DyNAM",
  layer = "calls",
  data = social_evolution
)
times_of(timed)
}
\seealso{
\code{\link[=simulate]{simulate()}}

Other simulation:
\code{\link[=set_parameter_provider]{set_parameter_provider()}},
\code{\link[=set_simulation_steps]{set_simulation_steps()}},
\code{\link{simulate}},
\code{\link{simulation-handle}}
}
\concept{simulation}
