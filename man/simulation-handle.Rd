% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_steps.R
\name{simulation-handle}
\alias{simulation-handle}
\alias{n_actors}
\alias{current_time}
\alias{process_map}
\alias{regime_of}
\title{Read a simulation handle inside a step}
\usage{
n_actors(x, side = 1L)

current_time(x)

process_map(x)

regime_of(x, fid = NULL)
}
\arguments{
\item{x}{the handle a simulation step was called with.}

\item{side}{which node set to count, \code{1} (senders) or \code{2} (receivers). They
differ only for a two-mode process.}

\item{fid}{a formula id from \code{process_map(x)}, or \code{NULL} for every process.}
}
\value{
\code{n_actors()} an integer; \code{current_time()} the handle's clock as a
number; \code{process_map()} the table of processes the walk carries, one row
per formula id; \code{regime_of()} a character vector naming each process's
regime — \code{"modeled"} (drawn from its own formula), \code{"completed"} (an
auto-supplied uniform or pinned default) or \code{"anchored-replay"} (an
unmodeled flavor replayed from the observed stream).
}
\description{
The \code{handle} a \code{\link[=set_simulation_steps]{set_simulation_steps()}} closure receives is opaque. These
accessors are what a step may read from it; the handle's other contents are
internal and may change between releases.
}
\examples{
# Inside a step closure:
clock <- function(rates, t, handle) {
  stopifnot(current_time(handle) <= t)
  list(wait = stats::rexp(1, sum(rates)), fid = 1L, kind = "event")
}
}
\seealso{
Other simulation:
\code{\link[=filter_simulation]{filter_simulation()}},
\code{\link[=set_parameter_provider]{set_parameter_provider()}},
\code{\link[=set_simulation_steps]{set_simulation_steps()}},
\code{\link{simulate}},
\code{\link[=times_of]{times_of()}}
}
\concept{simulation}
