% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_terms.R
\name{model_terms}
\alias{model_terms}
\alias{model_terms.goldfishFit}
\alias{model_terms.goldfishFlavFit}
\title{The terms of a fitted model, and what to call them}
\usage{
model_terms(x, pattern = NULL, ...)

\method{model_terms}{goldfishFit}(x, pattern = NULL, ...)

\method{model_terms}{goldfishFlavFit}(x, pattern = NULL, ...)
}
\arguments{
\item{x}{a fitted model of class \code{"goldfishFit"}, or a flavored
multi-process fit.}

\item{pattern}{an optional regular expression; only terms matching it in any
of their names or in their effect details are returned. Matching is
case-insensitive.}

\item{...}{additional arguments passed to or from other methods (currently
unused).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per term, carrying \code{index},
\code{term}, \code{coefficient}, \code{export}, the effect-detail columns, and — on a
flavored fit — \code{flavor} and \code{family}.
}
\description{
Every term of a fitted model as one row, carrying each of the names the
package will answer to for it. Use it when a term argument rejects what you
typed, when a model has too many effects for an error to list them, or when
two terms differ only in an argument.
}
\details{
goldfish renders a term differently depending on where it appears: the
printed summary shows a compact string, the export surfaces a
file-safe form, and \code{coef()} a minimal-unique abbreviation. Every argument
that takes a term — \code{\link[=set_algorithm_newton]{set_algorithm_newton()}}'s \code{initial_parameters}, the
\verb{effect =} / \verb{effects =} arguments of the \verb{diagnose_*} and \verb{test_*}
families — accepts \strong{any} of them, plus the coefficient's position. This
table is where you read them off.

Two of the spellings are unique by construction and two are not, which is
why more than one is accepted:
\describe{
\item{\code{term}}{the compact string the printed summary shows, e.g.
\verb{inertia/calls [15m]}. The most readable, but the summary \strong{abbreviates
it to the console width} — on a narrow console two terms can print
identically — and two terms differing only in an anonymous function
argument render alike. This column always holds the full, unabbreviated
string.}
\item{\code{coefficient}}{the \code{coef()} label, e.g. \code{inrt_cal_15m}. Unique by
construction: the disambiguating suffix is what makes it so.}
\item{\code{export}}{the file-safe form \code{\link[=gather_model_data]{gather_model_data()}} and
\code{tidy(compact = TRUE)} use.}
\item{\code{index}}{the coefficient's position. Unique, but \strong{fragile}: adding
a term to the formula renumbers every term after it.}
}

The remaining columns are the effect details the non-compact summary prints
— the object each effect reads, and the arguments it was given — so a
\code{pattern} search reaches them too.

On a flavored (multi-process) fit the per-process tables are row-bound with
\code{flavor} and \code{family} columns appended, so a term carried by two processes
appears once per process.

This is a \emph{fit-scoped} helper: it answers which terms this model has. It is
not the effect catalogue, which answers which effects goldfish provides.
}
\examples{
data("social_evolution")
fit <- estimate_dynam(
  calls ~ inertia + recip + trans,
  sub_model = "choice",
  data = social_evolution
)
model_terms(fit)

# Which of these is the one on the friendship network?
model_terms(fit, pattern = "recip")

}
\seealso{
\code{\link[=set_algorithm_newton]{set_algorithm_newton()}} for \code{initial_parameters}, and
\code{\link[=estimate_dynam]{estimate_dynam()}} for the fitted object these describe.
}
