.Rproj.user
.Rhistory
.RData
.Ruserdata
src/*.o
src/*.so
src/*.dll
docs/
*.Rproj
.Rprofile
review.txt
*.status
*.log
*.tmp
*.code-workspace
inst/doc
docs
.DS_Store
.positai
# Claude Code: share project tooling (settings.json hooks + agents); keep
# personal permission allow-lists and local worktrees out of git.
.claude/settings.local.json
.claude/worktrees/
# Personal memory: points at an Obsidian decision-record vault only I have.
CLAUDE.local.md

# OpenSpec: share config, the living folded spec, active proposals, AND the
# archive (changes/archive/ is tracked since ADR-0041 so decision memory
# travels with every clone and branch); keep the personal per-change session
# journals (progress*.md, at any depth — active or archived) and the
# parallel-session ownership claims local. Pre-2026-07 archives live in
# .plan/openspec/archive (local-only).
openspec/changes/**/progress*.md
openspec/changes/_claims/

# .plan is scratch; track only the cross-version benchmark ledger and the
# tooling that maintains it.
.plan/*
!.plan/goldfish_versions.csv
!.plan/profile_goldfish.R
!.plan/opsx-archive-track.sh
!.plan/opsx-spec-placement-check.sh
